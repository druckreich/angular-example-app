version: 2
jobs:
  build:
    working_directory: ~/app-name
    docker:
      - image: circleci/node:9.5.0
    steps:
      - checkout
      - restore_cache:
          keys:
            - dependency-cache-{{ checksum "package-lock.json" }}
            - dependency-cache-
      - run:
          name: check versions before update
          command: |
            node --version
            npm --version
      - run:
          name: update npm & @angular/cli
          command: sudo npm install -g npm@5.6.0 @angular/cli@1.6.8 --no-progress --quiet --loglevel warn
      - run:
          name: check versions after update
          command: |
            node --version
            npm --version
            ng --version
      - save_cache:
          key: dependency-cache-{{ checksum "package-lock.json" }}
          paths:
            - ~/app-name/node_modules
      - run:
          name: angular-build
          command: npm ng build --prod --loglevel warn
